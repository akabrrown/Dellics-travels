# Handoff — Web Restructure Plan (Tasks 14-17 remaining)

Resume execution of the plan at `docs/superpowers/plans/2026-08-25-web-restructure.md` using the superpowers `executing-plans` skill. Work continues on git branch `feat/web-restructure`.

## Where we stopped

Tasks 1-13 are COMPLETE and verified. Last commit: `4ef738d` ("feat(web): about, credentials, gallery pages with accessible lightbox").

Commit chain for reference: T6 `00cd536`, T7 `06eb634`, T8 `e35ad19`, T9 `cc1f998`, T10 `64b26ed`, T11 `00e6754`, T12 `cd808bb`, T13 `4ef738d`.

## What is left (plan line numbers)

- **Task 14 — Contact + inquire forms** (plan lines 3123-3307): `apps/web/app/contact/page.tsx` and `apps/web/app/inquire/page.tsx` — real validated forms that POST to the API. The API `InquiriesModule` already exists (Task 5, `apps/api`, Prisma `Inquiry` model, validation + throttling). Copy legacy body copy VERBATIM from `apps/Dellics Travels/Dellics Travels/pages/contact.html` and `inquire.html`.
- **Task 15 — Privacy, terms, admin stub, sitemap, robots** (plan lines 3307-3424).
- **Task 16 — Full verification** (plan lines 3424-3535): monorepo-wide gates (`pnpm exec turbo run check-types`, tests, builds for web + api) plus live checks.
- **Task 17 — Cutover** (plan lines 3535-3577): delete the legacy site `apps/Dellics Travels/`.
- After Task 17: use the superpowers `finishing-a-development-branch` skill.

## Open blockers to retry

1. **Supabase DB migration unapplied**: `supabase/migrations/20260825000000_create_inquiry_table.sql` (created in Task 3) failed with P1001/ENOTFOUND — DB was unreachable. Retry applying it before/during Task 14 and definitely before Task 16. Task 14 form persistence depends on this table.
2. **Ratehawk sandbox key**: was exposed in legacy files; the plan's Task 17 notes the user must rotate it. Remind the user at cutover.

## Hard rules (enforced throughout)

- **Verbatim copy**: all page text comes VERBATIM from legacy HTML in `apps/Dellics Travels/Dellics Travels/pages/`. Never paraphrase or invent. Legacy mojibake `?` / `ï¿½` = em-dash `—` or arrow `→`; render proper characters.
- **Images**: every `next/image` src must map to an existing kebab-case file under `apps/web/public` (verify with Glob before writing).
- **Commit discipline**: one commit per task, `git add apps/web` (or per plan), using the EXACT commit message written in the plan.
- **Verify each task** exactly as the plan specifies, typically from repo root: `pnpm exec turbo run check-types --filter=web; pnpm exec turbo run build --filter=web` (PowerShell — use `;` not `&&`).
- Follow each plan step in order; the plan contains full decision-complete code templates.

## Established conventions in apps/web

- Next.js 16.3 App Router (Turbopack), React 19, Tailwind v4, shadcn/ui. All pages static; dynamic routes use `params: Promise<{...}>` + `generateStaticParams` + `notFound()`.
- Brand tokens: `bg-navy`, `bg-ink`, `bg-brand-orange`, `text-slate-body`, `rounded-card`, `rounded-pill`, `font-display`.
- Shared components in `apps/web/src/components/`: `page-hero.tsx`, `content-sections.tsx` (`{ heading?, paragraphs, bullets? }[]`), `cta-banner.tsx` (`title` required; defaults `label="Start an inquiry"`, `href="/inquire"`), `section-heading.tsx`, `accreditation-strip.tsx`, `gallery/lightbox.tsx`.
- Data modules in `apps/web/src/data/` (`home.ts`, `destinations.ts`, `gallery.ts`).
- Standard page pattern: `PageHero` + `ContentSections` (verbatim sections) + `CtaBanner`; metadata title/description from legacy `<head>`.
- TypeScript has `noUncheckedIndexedAccess` on — guard array index access (see `gallery/lightbox.tsx` for the `openItem` pattern).
- Web dev server runs on port 3001, API on 3000. Never put Ratehawk credentials in `apps/web`.

## Current route table (21 routes, all static/SSG)

`/`, `/_not-found`, `/about`, `/corporate`, `/credentials`, `/destinations`, `/destinations/[region]` (africa, asia, europe, middle-east, north-america), `/diaspora`, `/flights`, `/gallery`, `/hotels`, `/services`, `/tours`, `/transfers`, `/visa`.

Tasks 14-15 will add `/contact`, `/inquire`, `/privacy`, `/terms`, `/admin` plus `sitemap.ts` and `robots.ts`.
